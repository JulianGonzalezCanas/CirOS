export interface Producto{
  color:string,
  price: number,
  ram: string, 
  storage: string, 
  quantity: number, 
  type: number,
  precio: number,
  id: number
}

export interface ProductSpecs {
  nombre: string;
  storage: number;
  color: string;
  ram: number;
}