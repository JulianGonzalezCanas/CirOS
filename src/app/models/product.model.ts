export interface Producto{
  color:string,
  price: number,
  ram: string, 
  storage: string, 
  quantity: number, 
  type: number
}

export interface ProductSpecs {
  nombre: string;
  storage: number;
  color: string;
  ram: number;
}